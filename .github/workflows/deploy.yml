name: Simulated Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Node Setup
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install Dependencies
        run: |
          cd backend
          npm install
          cd ../frontend
          npm install

      - name: Simulate Deployment
        run: |
          echo "Pretending to SSH to EC2"
          echo "Would run PM2 restart frontend & backend"
